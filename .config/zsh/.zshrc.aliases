source "$(dirname "$(dirname "$0")")/bash/.bashrc.aliases"

##
# Global aliases
alias -g G='| grep'
alias -g L='| less -R'
alias -g H='| head'
alias -g T='| tail'
alias -g S='| sort'
alias -g W='| wc'
alias -g X='| xargs'
